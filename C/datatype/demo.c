#include <stdio.h>
int main(){
    // Định nghĩa biến x;
    int x =10;
    // Định nghĩa biến y;
    int y = 20;
    // Thực hiện phép tính cộng
    int result = x+y;

    printf("Result = %d", result);
    return 0;
}